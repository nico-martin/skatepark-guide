import React from 'react';
import { Park } from '@app/vendor/types';

const ParkGallery = ({
  park,
  className = '',
}: {
  park: Partial<Park>;
  className?: string;
}) => <div />;

export default ParkGallery;
