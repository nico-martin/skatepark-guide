import React from 'react';
import { Park } from '@app/vendor/types';

const ParkContact = ({
  park,
  className = '',
}: {
  park: Partial<Park>;
  className?: string;
}) => <div />;

export default ParkContact;
