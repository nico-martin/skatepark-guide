import React from 'react';
import cn from '@common/utils/classnames';

const AccountParks = ({ className = '' }: { className?: string }) => {
  return <div className={cn(className)}>test</div>;
};

export default AccountParks;
